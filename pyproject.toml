[project]
name = "video-converter"
version = "0.1.0"
description = "Drag-and-drop video converter built with Django and ffmpeg"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "django>=6.0.4",
]
